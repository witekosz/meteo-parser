[tool.poetry]
name = "meteo-parser"
version = "0.1.0"
description = ""
authors = ["witekosz <witekosz@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
requests = "^2.25.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
